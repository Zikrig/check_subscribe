services:
  bot:
    build: .
    container_name: promo_bot_max
    restart: always
    env_file:
      - .env
    # store.json на хосте: ./data/store.json (рядом с проектом, переживает пересборку)
    environment:
      DATA_JSON_PATH: /app/data/store.json
      SSL_CERT_FILE: /etc/ssl/certs/ca-certificates.crt
    volumes:
      - .:/app
      - ./data:/app/data
    ports:
      - "8080:8080"
    dns:
      - 1.1.1.1
      - 8.8.8.8
